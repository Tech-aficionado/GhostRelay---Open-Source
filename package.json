{
  "name": "ghostrelay",
  "version": "1.1.0",
  "private": true,
  "description": "GhostRelay - Privacy-focused email aliasing service",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:worker": "cd worker && npx wrangler dev",
    "build:frontend": "cd frontend && npm run build",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "db:setup": "cd worker && npx wrangler d1 execute ghostrelay-db --local --file=../database/schema.sql",
    "install:all": "cd frontend && npm install && cd ../worker && npm install"
  }
}
